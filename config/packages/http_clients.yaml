
framework:
    http_client:
        scoped_clients:
            space.trader.client:
                base_uri: 'https://api.spacetraders.io/v2/'
                headers:
                    'Content-Type': 'application/json'
